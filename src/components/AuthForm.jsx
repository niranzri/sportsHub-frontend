/* import { useState } from "react";

const AuthForm = () => {

const [name, setName] = useState('');
const [email, setEmail] = useState('');
const [password, setPassword] = useState('');


    return ( 
      
        <form  style={{ display: 'flex', flexDirection: 'column' }}>
            <label>Name:
                <input value={name} onChange={event => setName (event.target.name)} /></label>
            <label>Email:
                <input value={email} onChange={event => setEmail (event.target.email)}/></label>
            <label>Password:<input value={password} onChange={event => setPassword (event.target.password)} /></label>
            <button type='submit'></button>
        </form>
       
     );
}
 
export default AuthForm; */